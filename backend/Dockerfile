# Eanfaches MavencIms Maage für Spring BEntwit-lun
# Arbeitsverzeichnis festlegen
WORKDIR /app

# Kopiere pom.xml und den Quellcode
COPY pom.xml .
COPY src ./srcQllcode

# Umgebungsvann
RUN mkdir -p /root/.m2
ENV SPRING_PROFILES_ACTIVE=dev

# Maven-Cache-Verzeichnis erstellenRUN mkdir -p /root/.m2

 imEntwicklungmodus smnspringboot:un-Dsring-boot.runpofiles=dev